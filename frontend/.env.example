# the SPA always calls the api on the same origin it's served from. the dev
# server proxies /v1/* to the dockerized api (see vite.config.ts), and the
# production image bakes the SPA into the api container itself, so a relative
# /v1 covers both. only override this if you're hosting the api elsewhere.
VITE_API_BASE_URL=/v1
